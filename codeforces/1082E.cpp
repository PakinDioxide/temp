#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    int n;
    cin >> n;
    int a[n];
    for (int i = 0; i < n; i++) cin >> a[i];
    
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t; t = 1; while (t--) solve();
}