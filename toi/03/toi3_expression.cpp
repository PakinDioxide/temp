#include <bits/stdc++.h>

using namespace std;

int main() {
    string s;
    cin >> s;
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        string ss = s;
        int k;
        cin >> k;
        while (k != 0) {
            int c = 0;
            for (int i = 0; i < ss.size() && c < k; i++) {
                
            }
        }
    }
}