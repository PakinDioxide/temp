#include "OES.h"
#include <vector>

const ll mxN = 

vector <pair <int, ll>> adj[mxN];

int machinepower(int N, std::vector<int> U, std::vector<int> V, std::vector<int> L) {
    return 0;
}

/*
do centroid decompo

we do dfs

up to node u

sum the odd, even
*/