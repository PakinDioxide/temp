#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);

    int n, m;
    cin >> n >> m;

    int a[m][2];
    for (int i = 0; i < m; i++) cin >> a[i][0] >> a[i][1];

    for (int i = 0; i < m; i++) {
        int r = a[i][0], c = a[i][1];
    }
}
