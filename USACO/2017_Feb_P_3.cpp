#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    // freopen("friendcross.in", "r", stdin);
    // freopen("friendcross.out", "w", stdout);
    int n, k; cin >> n >> k;
    pair <int, int> id[n+1];
    for (int i = 1, x; i <= n; i++) cin >> x, id[x].first = i;
    for (int i = 1, x; i <= n; i++) cin >> x, id[x].second = i;
    for (int i = 1; i <= n; i++) {
        
    }
}