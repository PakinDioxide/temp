#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int n; cin >> n;
    ll a[n], m; for (auto &e : a) cin >> e;
    cin >> m;
    ll l = 0, r = 1e18, k = -1;
    while (l <= r) {
        
    }
}