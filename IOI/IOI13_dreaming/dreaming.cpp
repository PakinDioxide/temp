#include <bits/stdc++.h>
#include "dreaming.h"

using namespace std;

int travelTime(int n, int m, int l, int a[], int b[], int t[]) {
    int par[n], rnk[n];
    iota(par, par+n, 0);
    memset(rnk, 0, sizeof(rnk));
    
}
