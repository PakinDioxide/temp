#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    int n, k, q; cin >> n >> k >> q;
    
    for (int i = 1; i < (1 << k); i++) {

    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t; cin >> t;
    while (t--) solve();
}