#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    int n, m, k;
    cin >> n >> m >> k;
    int a[n][m];
    priority_queue <tuple <ll, int, int, int>>
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) cin >> a[i][j];
    }
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();
}