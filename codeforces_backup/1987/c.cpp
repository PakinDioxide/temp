#include <bits/stdc++.h>

using namespace std;

#define int long long

int32_t main() {
    int t;
    scanf("%lld", &t);
    while (t--) {
        int n;
        scanf("%lld", &n);
        int a;
        int r = 0, mx = 0;
        for(int i = 0; i < n; i++){
            scanf("%lld", &a);
            
        }
        printf("%lld\n", );
    }
}