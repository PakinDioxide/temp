#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    int n, m;
    cin >> n >> m;
    int c[2*n][m]
    for (int i = 0; i < 2*n; i++) for (int j = 0; j < m; j++) c[i][j] = 1;
    for (int i = 0; i < m; i++) {
        for (int j = i+1; j < m; j++) {
            
        }
    }
}

int32_t main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();
}