#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n;
    scanf("%d", &n);
    vector <int> a(n), b(n);
    for (int i = 0; i < n; i++) scanf("%d", &a[i]);
    
}

int main() {
    int t;
    scanf("%d", &t);
    while (t--) solve();
}