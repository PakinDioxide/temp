#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int a[n];
    for (int i = 0; i < n; i++) cin >> a[i];
    sort(a, a+n);
    int l = 0, r = INT_MAX, ans = 0;
    while (l <= r) {
        int mid = (l+r)/2;
        
    }
}