#include <bits/stdc++.h>

using namespace std;

int n;

int main() {
    scanf("%d", &n);
    vector <pair <int, int>> x[n], y[n];
    for (int i = 0; i < n, i++) {
        int x1, y1, x2, y2;
        scanf("%d %d %d %d", &x1, &y1, &x2, &y2);
        x[i] = 
    }
}