#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    ll l, r, i, k;
    cin >> l >> r >> k >> k;
    
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();
}