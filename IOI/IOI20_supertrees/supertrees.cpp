#include "supertrees.h"
#include <bits/stdc++.h>
using namespace std;

int construct(vector <vector <int>> p) {
    int n = p[0].size()
	vector <vector <int>> cnt(n, vector <int>(n));
    for (int k = 0; k < n; k++) {
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++)
        }
    }
    
}