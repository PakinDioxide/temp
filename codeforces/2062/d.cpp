#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    int n;
    cin >> n;
    vector <int> adj[n+5];
    ll l[n+5], r[n+5];
    for (int i = 1; i <= n; i++) cin >> l[i] >> r[i];
    
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();
}