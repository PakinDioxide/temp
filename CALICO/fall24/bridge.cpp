#include <bits/stdc++.h>

using namespace std;

void solve() {
    int b, n;
    cin >> b >> n;
    int a[n];
    for (int i = 0; i < n; i++) cin >> a[i];
}

int main() {
    ios::sync_with_stdio(0), cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();
}