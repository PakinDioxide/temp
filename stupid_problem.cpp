#include <bits/stdc++.h>
#define ll long long

using namespace std;

void solve() {
    ll n, m, k;
    cin >> n >> m >> k;
    
}

int main() {
    int t;
    cin >> t;
    while (t--) solve();
}

/*
if k is good -> find 2 last person -> a, b; if (b == k) ans = a, else ans = b;
else reduce the
*/