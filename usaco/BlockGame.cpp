#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;

    cin >> n;

    vector <pair <string, string>> a(n);

    for (int i = 0; i < n; i++) cin >> a[i].first >> a[i].second;

    int c[26];
    for (int i = 0; i < 26; i++) c[i] = 0;


}
